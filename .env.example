# ========================================
# 慢性病诊疗方案推荐系统 - 环境变量配置示例
# ========================================
#
# 使用说明：
# 1. 复制本文件为 .env：cp .env.example .env
# 2. 根据实际情况修改配置项
# 3. 不要将 .env 文件提交到版本控制系统
#
# ========================================

# ========================================
# 服务器端口配置
# ========================================

# 后端服务器地址
# 默认：127.0.0.1（本地开发）
# 生产环境可以设置为 0.0.0.0（监听所有网络接口）
BACKEND_HOST=127.0.0.1

# 后端服务器端口
# 默认：8001
BACKEND_PORT=8001

# 前端开发服务器端口
# 默认：5173（Vite 默认端口）
FRONTEND_PORT=5173

# 前端 API 地址（开发环境）
# 本地开发：http://127.0.0.1:8001
# 生产环境：留空或删除此行（使用相对路径通过 nginx 代理）
VITE_API_BASE_URL=http://127.0.0.1:8001

# ========================================
# 数据库配置
# ========================================

# SQLite 数据库文件路径
# 默认：sqlite:///./cdhcprs.db（backend 目录下的 cdhcprs.db 文件）
# 生产环境建议使用绝对路径
DATABASE_URL=sqlite:///./cdhcprs.db

# ========================================
# JWT 认证配置
# ========================================

# JWT 密钥（必填）
# 重要：请生成一个随机字符串，不要使用默认值！
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-please-change-in-production

# JWT 加密算法
# 默认：HS256
ALGORITHM=HS256

# JWT Token 过期时间（分钟）
# 默认：30 分钟
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========================================
# 应用配置
# ========================================

# 应用名称
# 默认：慢性病诊疗方案推荐系统
APP_NAME=慢性病诊疗方案推荐系统

# 调试模式
# 开发环境：True
# 生产环境：False
DEBUG=True

# ========================================
# 注意事项
# ========================================
#
# 1. SECRET_KEY 必须修改为随机字符串
# 2. 生产环境请将 DEBUG 设置为 False
# 3. 修改端口后需要重启服务才能生效
# 4. 生产环境请将 VITE_API_BASE_URL 留空或删除
# 5. LLM 配置可以在管理后台动态修改
# 6. 更多配置项请参考文档
#
# ========================================
